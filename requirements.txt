# PDF extraction
pdfplumber>=0.11
pymupdf>=1.24
pytesseract>=0.3       # OCR fallback for scanned PDFs (needs: brew install tesseract)
Pillow>=10.0

# Vector database and embeddings
chromadb>=1.0
sentence-transformers>=3.0

# LLM API (free tier)
groq>=0.9              # LLaMA 3.3 70B and Qwen 3 32B via Groq (console.groq.com)

# Optional paid LLM APIs (not used by the default pipeline)
anthropic>=0.28
openai>=1.30
tiktoken>=0.7

# Data, visualisation, deliverable
tqdm>=4.66
pandas>=2.2
numpy>=1.26
matplotlib>=3.8
seaborn>=0.13
python-docx>=1.1       # build_deliverable_report.py generates the .docx
jupyter>=1.0
ipykernel>=6.0
